import styled from 'styled-components'

export default styled.section`
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    padding-left: max(calc(50% - 730px / 2), 2rem);
    padding-right: max(calc(50% - 730px / 2), 2rem);
`
