import styled from 'styled-components'

export default styled.section`
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-left: max(calc(50% - 1100px / 2), 2rem);
    padding-right: max(calc(50% - 1100px / 2), 2rem);
`
